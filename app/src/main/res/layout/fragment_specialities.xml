<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/specialities_containier"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.specialities.SpecialitiesFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbarSize="@dimen/scrollbarSize"
        android:scrollbarTrackVertical="@android:color/white"
        android:scrollbars="vertical"
        tools:listitem="@layout/card_profession" />

</FrameLayout>